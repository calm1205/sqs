[project]
name = "worker"
version = "0.1.0"
description = "Worker service for processing SQS messages"
requires-python = ">=3.14"
dependencies = [
    "boto3>=1.35.86",
]

[dependency-groups]
dev = [
    "boto3-stubs[sqs]>=1.35.86",
    "mypy>=1.14.1",
    "ruff>=0.8.6",
]
